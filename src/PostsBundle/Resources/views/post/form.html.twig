{{ form_start(form,{ 'action': path('post_new'), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    {{ form_row(fileForm.file) }}
    <input type="submit" value="Create"/>
{{ form_end(form) }}
