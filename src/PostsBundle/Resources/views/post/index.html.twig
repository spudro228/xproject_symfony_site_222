{% extends 'base.html.twig' %}


{% block body %}
    <style>
        table {
            border: 4px double black; /* Параметры границы */
            background: #CCCCCC; /* Цвет фона */
            padding: 10px; /* Поля вокруг текста */
        }

        .go {
            border: none;
        }

        li {
            list-style-type: none;
            border: 1px solid black; /* Убираем маркеры */
            margin: 10px;
        }

        .post {
            margin: 10px;
        }
    </style>
    <h1>Posts list</h1>

    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Autor</th>
            <th>Title</th>
            <th>Text</th>
        </tr>
        </thead>
        {#<tbody>
        {% for post in posts %}
            <tr>
                <td><a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.id }}</a></td>
                <td>{{ post.autor }}</td>
                <td>{{ post.title }}</td>
                <td>{{ post.text }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('post_show', { 'id': post.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('post_edit', { 'id': post.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>

        {% endfor %}
        </tbody>#}
    </table>
    <ul>
        {% for post in posts %}
            <li>
                <div class="post">
                    <span><a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.id }}</a></span>
                    <span>{{ post.author }}</span>
                    <span>{{ post.title }}</span>
                    <br/>
                    {{ post.text }}

                    <ul>
                        <li class="go">

                            {# Заменить на кнопки#}
                            <span><a href="{{ path('post_edit', { 'id': post.id }) }}">edit</a></span>
                            <span><a href="{{ path('post_show', { 'id': post.id }) }}">show</a></span>
                        </li>
                    </ul>
                </div>
            </li>

        {% endfor %}
    </ul>

    <ul>
        <li>
            <a href="{{ path('post_new') }}">Create a new post</a>
        </li>
    </ul>
{% endblock %}
